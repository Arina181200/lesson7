package com.company;

public class Car extends Shop {
    public Car(int number, String name, int cost, int height, int width, int length) {
        super(number, name, cost, height, width, length);
    }
}
